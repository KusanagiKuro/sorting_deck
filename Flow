1/ Write down commands when gui is enabled
2/ Initialize the window:
  + A window with background
  + List of cells:
    . Each cell containing the number, their index, and the border
  + An area to explain the color meaning
  + A tutorial
3/ Run each step:
  + Read each line
  + Decode the command and send out the correct function

NEED:
- A decoder:
  + Read the command and decode
  + Send out the parameters needed for the function
- A controller:
  + Receive the parameter needed
  + Use its functions to control the movement of all elements on screens
